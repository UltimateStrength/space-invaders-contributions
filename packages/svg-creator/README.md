# @space-invaders-contributions/svg-creator

Generate a svg file from the grid and the ship's path.

Use css style tag to animate the ship, the lasers and the grid cells. For that reason it only work in browser. Animations are likely to be ignored be native image reader.
